# Development Dockerfile for React (with Vite or CRA)
FROM node:24-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy the rest of the code
COPY . .

# Expose Vite or CRA dev port
EXPOSE 5173

CMD ["npm", "run", "dev"]
